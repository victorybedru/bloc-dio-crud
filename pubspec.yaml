name: bloc_dio_crud
description: A Flutter CRUD app with BLoC and Dio
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'  # Add this line!

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  flutter_bloc: ^8.1.4
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true